# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.93"
name = "near-sdk-env"
version = "0.1.1"
authors = ["Near Inc <hello@near.org>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = """
Low-level abstraction over `near-sys` host functions. Provides the same
hashing (and related) API on wasm32 (via NEAR VM host calls) and on 
non-wasm32 (via pure-Rust crates).
"""
homepage = "https://docs.near.org/"
readme = "README.md"
categories = ["wasm"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/near/near-sdk-rs"

[features]
__near-sdk-unit-testing = ["dep:near-sys"]

[lib]
name = "near_sdk_env"
path = "src/lib.rs"

[dependencies.near-sys]
version = "~0.2.10"
optional = true

[dev-dependencies.hex]
version = "0.4"

[target."cfg(near)".dependencies.near-sys]
version = "~0.2.10"

[target."cfg(not(near))".dependencies.ripemd]
version = "0.1.1"

[target."cfg(not(near))".dependencies.sha2]
version = "0.10"

[target."cfg(not(near))".dependencies.sha3]
version = "0.10"

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = ["cfg(near)"]
